# Vox Navigator - TTS Server

Local FastAPI backend for neural text-to-speech processing.

## Overview

This server provides:
- Neural TTS processing
- GPU-accelerated inference (CUDA, ROCm, CPU fallback)
- Local-only execution
- WAV audio output

## Installation

See the main project README for installation instructions.
